# Random Forest and Balanced Classification Workflow

## Files

- Notebook: `random_forest_imbalanced_classification.ipynb`
- HTML view: `random_forest_imbalanced_classification.html`
- This README: `random_forest_imbalanced_classification_README.md`

## Overview

Random Forest-based imbalanced classification workflow. The notebook performs data cleaning, feature importance ranking, top-k feature selection, resampling, balanced random forest modelling, randomised hyperparameter search, threshold optimisation, and evaluation.

## What the notebook contains

This notebook contains **29 cells**: 0 markdown cells, 29 code cells, and 15 saved outputs. The HTML view was generated from the existing notebook content so reviewers can inspect the work without relying on GitHub's notebook preview.

## Methods and tools shown

- Random Forest and Balanced Random Forest
- Feature importance ranking
- Top-k feature subset testing
- Random undersampling
- Randomised hyperparameter search
- Threshold optimisation for F1 and minority-class recall

## Portfolio relevance

Strong supporting evidence for data analyst and data science roles because it shows practical model improvement and evaluation beyond simple accuracy.

## Notes from review

A duplicate/misnamed copy is kept only in the archive folder and should not be submitted as a lead project.

## How to view

For GitHub review, open this README first. If the `.ipynb` preview appears blank or slow, download/open `random_forest_imbalanced_classification.html` locally or serve it through GitHub Pages. The notebook file is retained for technical review and reproducibility.
